pipeline:
    docker:
        image: plugins/docker
        dockerfile: Dockerfile
        repo: sirboops/elixir
        tags: 1.8.2-alpine
        experimental: true
        squash: true
        secrets: [ docker_username, docker_password ]
